import { Object3D } from 'three';

/**
 * Avaturn Avatar Loader (secondary option)
 */
export async function loadAvaturnAvatar(avaturnUrl: string): Promise<Object3D> {
  // This would require Avaturn SDK
  // For MVP, we'll provide a stub that can be extended
  throw new Error('Avaturn loader not yet implemented. Use Ready Player Me for MVP.');
}

